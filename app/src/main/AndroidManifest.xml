<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.itda">

    <application
        android:allowBackup="true"
        android:name=".MyApplication"
        android:icon="@mipmap/ic_itda_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".HomePage" />
        <activity android:name=".setting" />
        <activity android:name=".BoardPage" />
        <activity android:name=".SearchNeologism" />
        <activity android:name=".SearchBoard" />
        <activity android:name=".BoardDetail" />
        <activity android:name=".SignUp" />
        <activity android:name=".NeologismRequest" />
        <activity android:name=".SearchBoardDetail" />
        <activity android:name=".WritePage" />
        <activity android:name=".Neologism_page" />
        <activity android:name=".AddImagePage" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>